# Week 2 – Smoke Test

## Result

- `docker compose up -d` completed without errors
- PostgreSQL container reached **healthy** status
- App container started successfully and remained running
- No unexpected container exits observed

## Commands executed
```bash
docker compose up -d
docker compose ps
